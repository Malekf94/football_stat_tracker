# Core AI/ML
torch>=2.3.0
torchvision>=0.18.0
ultralytics>=8.2.0
supervision>=0.21.0

# Computer Vision
opencv-python>=4.9.0

# Team classification — Roboflow `sports` (SigLIP + UMAP + KMeans)
# Installed from GitHub; pulls transformers/umap-learn below.
# NOTE: install with --no-deps so it can't replace the CUDA torch build:
#   pip install --no-deps git+https://github.com/roboflow/sports.git
transformers>=4.40.0
umap-learn>=0.5.5
sentencepiece>=0.2.0          # required by the SigLIP tokenizer
protobuf>=4.25.0              # required by the SigLIP tokenizer

# Data / Stats
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
openpyxl>=3.1.0

# Utilities
tqdm>=4.65.0

# GUI drag-and-drop
tkinterdnd2>=0.4.0
